function make_shirt(size, message) {
    console.log("The shirt size is ".concat(size, " and it should have the message: \"").concat(message, "\"."));
}
// Call the function
make_shirt('Large', 'Hello, World!'); // You can pass different size and message values here
